# qrcodegen (vendored)

QR Code generator library (C++) © Project Nayuki — **MIT License**
https://www.nayuki.io/page/qr-code-generator-library

Вендорено без изменений (`qrcodegen.hpp` / `qrcodegen.cpp`) для рендера QR-якорей в
ПК-клиенте (`src/QrImage.cpp`). Полный текст лицензии — в заголовке каждого файла.
