{
  "name": "smartstack-messenger-server",
  "version": "1.0.0",
  "description": "Self-hosted end-to-end encrypted messenger server (Fastify + Prisma).",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.0",
    "@fastify/multipart": "^8.3.0",
    "@prisma/client": "^5.18.0",
    "bcryptjs": "^2.4.3",
    "fastify": "^4.28.0",
    "prisma": "^5.18.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
